# 复制到本目录 .env.development 后执行 npm run dev
#
# 管理后台登录走 /mlk/platform/v1 → 必须能访问 **Platform**。
# - 本机双进程：填 http://127.0.0.1:8081（不要填 8080 Gateway，否则登录会失败）
# - 经 Nginx 公网：填 http://<IP>:8100（同端口含 /mlk/v1 与 /mlk/platform）
VITE_PROXY_TARGET=http://127.0.0.1:8081
